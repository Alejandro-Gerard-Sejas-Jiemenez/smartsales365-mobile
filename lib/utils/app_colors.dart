import 'package:flutter/material.dart';

class AppColors {
  // Colores principales
  static const Color primary = Color(0xFF0A84FF);
  static const Color secondary = Color(0xFF22C55E);
  static const Color accent = Color(0xFFEF4444);

  // Colores de fondo
  static const Color background = Colors.white;
  static const Color surface = Color(0xFFF8FAFC);
  static const Color cardBackground = Colors.white;

  // Colores de texto
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textLight = Color(0xFF94A3B8);

  // Colores de estado
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Colores de bordes y divisores
  static const Color border = Color(0xFFE2E8F0);
  static const Color divider = Color(0xFFE2E8F0);

  // Colores para el carrito
  static const Color cartBadge = Color(0xFFEF4444);
  static const Color cartButton = Color(0xFF0A84FF);
  
  // Colores para el dashboard
  static const Color chartLine = Color(0xFF0A84FF);
  static const Color chartBackground = Color(0xFFE0F2FE);
  static const Color predictionPositive = Color(0xFF22C55E);
  static const Color predictionNegative = Color(0xFFEF4444);
}